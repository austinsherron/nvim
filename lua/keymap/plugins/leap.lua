-- TODO: figure out why custom leap mappings leave cursor one or two chars off target

-- local km = require 'utils.core.mapper'


-- TODO: refactor KeyMapper so that it can be instantiated w/ the state present in this
--       function
-- local function options(desc)
--   return { desc = 'leap: ' .. desc, nowait = true, silent = true }
-- end


-- interactions ----------------------------------------------------------------

-- note: see lua/plugins/motion > ggandor/leap.nvim > config for default mappings

-- km.nnoremap(';', '<Plug>(leap-forward-till)',  options('forward'))
-- km.nnoremap(',', '<Plug>(leap-backward-till)', options('backward'))

